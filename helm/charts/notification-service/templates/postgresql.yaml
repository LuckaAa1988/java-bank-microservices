apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-db
  template:
    metadata:
      labels:
        app: notification-db
    spec:
      containers:
        - name: notification-db
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "notification" # или valueFrom.secretKeyRef
            - name: POSTGRES_USER
              value: "user" # или valueFrom.secretKeyRef
            - name: POSTGRES_PASSWORD
              value: "12345"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          emptyDir: { }

---
apiVersion: v1
kind: Service
metadata:
  name: notification-db
spec:
  type: ClusterIP
  selector:
    app: notification-db
  ports:
    - port: 5432
      targetPort: 5432